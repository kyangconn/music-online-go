# Music Online Pages

基于 Vue 3 + TypeScript + Element Plus 的前端单页应用，用于 Music Online 项目的 Web 界面。

## 开发

```bash
npm install
npm run dev
```

访问 `http://localhost:5173`。

## 构建

```bash
npm run build
```

构建产物默认输出到 `../cmd/server/dist`，由后端 Go 服务嵌入并对外提供。
