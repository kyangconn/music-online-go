services:
  app:
    environment:
      INTEGRATIONS_MUSICBEE_SUBMIT_TOKEN: ""
      INTEGRATIONS_MUSICBEE_SUBMIT_TOKEN_FILE: /run/secrets/musicbee_submit_token
    secrets:
      - musicbee_submit_token

secrets:
  musicbee_submit_token:
    file: ${MUSICBEE_SUBMIT_TOKEN_HOST_FILE:-./secrets/musicbee_submit_token}
