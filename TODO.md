# Music Online TODO

目标：做出一个能部署、能访问、适合个人自用的成品。先保证核心路径可靠，再考虑扩展功能。

## 当前功能地图

- 用户：注册、登录、资料更新、改密码、删除账号、JWT 认证。
- 安全：TOTP 设置、启用、禁用；管理员权限会实时查数据库角色和账号状态。
- 音乐：搜索列表、详情、创建记录、上传音频/封面、流式播放、喜欢/取消喜欢、按用户查看上传和喜欢。
- 导入：单曲上传、浏览器本地目录批量扫描、音频元数据解析、批量上传。
- 标签：音乐标签 CRUD、搜索、模糊匹配、MusicBrainz ID 查询、MusicBee 风格 track/search 和 track/submit。
- 管理：系统信息面板、后端用户列表/状态/角色接口、管理员删除音乐接口。
- 运维：Go + Vue 构建为单二进制；支持 SQLite/PostgreSQL；健康检查、Prometheus metrics、Dockerfile。

## P0：先修核心可用性

- [x] 修复音乐播放路径响应：`Music.ToResponse()` 没有返回 `Path`，但上传会写入 `Music.Path`，`/musics/:id/stream` 也依赖该字段；当前详情页可能一直显示“无音频”。
- [x] 给音乐上传/播放补集成测试：创建音乐、上传临时音频、详情返回可播放标记、`/stream` 返回 200 和正确内容。
- [x] 补音乐修改/删除权限：当前 handler 有 TODO，登录用户可能修改或删除别人的音乐；至少限制为上传者或管理员。
- [x] 统一注册密码规则：前端注册页最小 6 位，后端注册要求 8 位，改密码后端又是 6 位；需要统一为一个规则并同步提示文案。
- [x] 固化个人部署默认方案：建议默认 SQLite 文件数据库，不使用 `:memory:`；明确上传目录、数据库文件、配置文件的持久化位置。
- [x] 修正 Docker 部署约束：Dockerfile 直接 `COPY config.yaml`，但 `config.yaml` 不应提交；改为复制 `config-example.yaml`、环境变量配置，或文档要求构建上下文提供配置。

## P1：成品稳定性和维护性

- [x] 增加分页参数校验：`page <= 0` 或异常 `page_size` 会传到 repository 计算 offset/limit，应该统一限制范围。
- [x] 统一 API 响应格式：用户/音乐接口使用 `{ code, message, data }`，标签接口大量直接返回裸对象或 `gin.H`；前后端类型会更难维护。
- [x] 修正标签搜索契约：路由是 `POST /music-tags/search`，handler 却用 `ShouldBindQuery`；决定用 GET query 还是 POST JSON，并同步测试。
- [x] 检查 TOTP 的产品语义：当前能启用/禁用 TOTP，但登录流程没有要求输入 TOTP code；如果要保留双因素，需要补完整登录校验，否则 UI 文案应降级。
- [x] 补管理员前端能力：后端已有用户列表、禁用/解禁、角色修改、删除音乐接口，但前端管理页目前主要是系统信息。
- [x] 清理包管理器痕迹：仓库有 `web/package-lock.json`，但项目约束要求只用 pnpm；确认无用后删除并在 CI/文档里强调。
- [x] 给 Makefile/README 对齐：README 写 `make lint` 是前端 ESLint，但 Makefile 实际会 `go fmt ./...`、`go vet ./...`、再跑前端 lint。

## P2：自用体验改进

- [x] 音乐编辑页：修改标题、艺术家、简介、封面/音频，支持删除自己上传的音乐。
- [x] 批量导入结果页：显示成功、失败、跳过原因，失败项可重试；已创建记录的失败项复用原 ID。
- [x] 重复音乐处理：前后端 SHA-256 精确匹配，启动时回填旧记录哈希；标题+作者疑似匹配，重复文件跳过，并在两边元数据不完整时填补空字段。
- [x] 首页筛选：按艺术家、年份、类型、是否喜欢在后端查询后分页，筛选状态同步到 URL。
- [x] 封面访问方式整理：现在封面路径是本地磁盘路径，浏览器不一定能直接访问；需要统一封面静态访问或封面流接口。
- [x] 上传文件校验：限制音频/图片 MIME、扩展名、大小；避免误传超大文件或非媒体文件。
- [x] 数据备份说明：给 SQLite 数据库、上传目录、配置文件写一个最短备份/恢复步骤。
- [x] 首个管理员创建方式：明确通过配置、命令、脚本或数据库初始化创建 admin，避免部署后只能注册普通用户。

## P3：前端现代化和成品细节

原则：只在能改善具体页面、上传流程或播放体验时引入新浏览器能力；不为了追新技术改架构。

### A：近期值得落地

- [x] Container Queries：首页/搜索结果、个人页卡片和全局播放器按自身容器宽度调整，嵌套布局不再只依赖 viewport 断点。
- [x] CSS Subgrid：首页和搜索结果卡片共用父网格行轨道，封面、标题、作者和喜欢数在不同卡片间对齐。
- [x] Web Worker 文件预处理：把批量导入里的音频元数据解析、文件 hash、疑似重复预检放到 worker，避免大目录扫描时卡住主线程。
- [x] Media Session API：播放音乐时写入标题、作者、封面，并接入系统媒体键、锁屏/通知区控制；这是音乐类 Web App 最像成品的细节之一。
- [x] 全局迷你播放器/播放队列：离开详情页后仍能看到当前播放、暂停/下一首/进度；和 Media Session API 一起做。

### B：中期增强

- [x] `:has()` 状态样式：批量预处理出现疑似重复项时由后代状态调整表格边框；只用于样式表达，不承载业务状态。
- [x] View Transition API：页面之间使用支持降级和减少动态效果偏好的轻量淡入淡出，不引入复杂动画系统。
- [x] PWA 基础能力：补 manifest、图标、主题色、安装入口和离线应用壳；自部署时可以像桌面/移动应用一样从系统入口打开。
- [x] File System Access API 渐进增强：支持时用 `showDirectoryPicker()` 优化目录导入，保留 input/webkitdirectory 兼容路径。
- [x] 本地持久存储策略：用 localStorage 保存播放队列、最近播放进度和单曲上传文本草稿；搜索筛选保留在 URL，文件本体不做本地持久化。
- [x] 快捷键：空格播放/暂停、左右快退快进、`/` 聚焦搜索、`u` 进入上传；避开输入框和可交互控件。
- [x] 最近播放/继续播放：记录上次播放歌曲和进度，首页提供恢复入口。

### C：观察或暂缓

- [ ] CSS Anchor Positioning：适合以后做自定义浮层、上下文菜单、歌词时间轴提示；当前 Element Plus 已覆盖大部分弹层，不急。
- [ ] Popover API / `<dialog>`：原生能力不错，但项目已有 Element Plus 弹窗体系；除非要减少依赖或做很轻的确认框，否则暂缓。
- [ ] Web Audio API：可做频谱、响度预览、波形进度条；视觉收益高但不是核心功能，等播放队列和重复检测稳定后再考虑。
- [ ] WebCodecs / WebGPU：对个人音乐管理系统收益很低，暂不纳入近期实现。

## 推荐执行顺序

1. 修 P0 的播放、权限、部署配置三件事。
2. 补一组后端集成测试覆盖注册登录、上传播放、权限、管理员系统信息。
3. 再补前端管理/编辑能力，让自用闭环完整。
4. 最后做批量导入体验、重复检测、备份说明等舒适度增强。
5. 前端现代化优先从 Web Worker 预处理、全局播放器、Container Queries/Subgrid 开始；它们最贴近当前痛点。

## 工作约束

- 前端只用 pnpm，优先通过根目录 Makefile 执行命令。
- 不删除 `cmd/server/dist/`，除非明确需要重新生成或用户要求。
- 新增 UI 文案走 `web/src/i18n/`，同时补中文和英文。
- 不引入新的前端 UI 框架，继续使用 Element Plus、Pinia、Vue Router。
- `config-example.yaml` 的变更需要同步 README。

## 2026-07-11 审计确认项

以下项目已经由当前代码路径或工具输出确认。安全与数据一致性问题优先于新增功能。

### P0：发布前安全阻断

- [x] 禁止客户端直接写入音乐的 `img` / `path`：创建和更新 DTO 当前接受任意服务器路径，播放和封面接口随后会直接 `os.Open`；应只允许上传流程生成路径，并校验最终路径始终位于上传根目录内，同时补目录穿越和任意文件读取回归测试。
- [x] 启动时校验 JWT secret：空值和示例值 `your-secret-key-change-in-production` 当前都能进入运行态；生产模式必须拒绝弱值并给出可操作错误。
- [x] 让禁用账号的现有 JWT 立即失效：普通鉴权中间件目前只验证 token，不查询账号状态；至少对敏感写操作实时检查，或引入可撤销会话/令牌版本。

### P1：数据一致性与运维

- [x] 上传替换改为原子流程：当前先覆盖最终文件再更新数据库；数据库失败时，同名旧文件无法恢复，不同扩展名的旧文件还会残留。应使用临时文件、原子 rename、旧文件延迟清理和失败恢复。
- [ ] 定义音乐和账号删除生命周期：当前以软删除数据库记录为主，上传文件、喜欢关系、用户拥有的媒体及保留/转移策略没有完整闭环。
- [ ] 用版本化 migration 取代仅靠 GORM `AutoMigrate` 的升级方式，并提供备份、升级失败和回滚说明。
- [x] 收紧本地数据权限：SQLite 目录和上传目录当前创建为 `0755`；自托管默认应按数据敏感度使用 `0700` / `0750`，并允许配置覆盖。
- [x] 修正 CORS：当前同时返回 `Access-Control-Allow-Origin: *` 与 credentials；改为明确的允许来源列表，并补预检和跨域凭据测试。
- [x] 区分存活与就绪检查：`/health` 当前不检查数据库和存储；增加 readiness 检查，并决定 `/metrics` 是否需要鉴权或仅绑定内网。

### P2：可靠性与前端质量

- [x] 登录只在"用户名不存在"时回退邮箱查询；当前任意用户名查询错误都会被吞掉并伪装成凭据错误，数据库故障无法被正确观测。
- [x] 防止管理员禁用/降级自己或最后一个有效管理员，避免小范围部署把唯一管理入口锁死。
- [x] 完整处理上传签名读取的关闭错误，并用 `errors.Is` 判断 `io.EOF` / `io.ErrUnexpectedEOF`。
- [x] 容错读取用户 `localStorage`：损坏的 `user` JSON 当前会在 Pinia store 初始化时直接抛错，可能导致应用无法启动。
- [x] 清理 `SecuritySettings` 中硬编码英文错误和图片替代文本，统一走中英文 i18n。
- [x] 改为 Element Plus 按需引入或拆分重模块；当前全量注册导致构建出的主请求 chunk 约 1.17 MB，影响首次加载。

### 已确认测试缺口

- [x] Go handler 集成测试：禁用账号拦截、管理员自保护、权限隔离、路径穿越、健康就绪（handler_test 12→18 func）
- [x] Middleware 单元测试：CORS Origin 反射、角色中间件（新增 middleware_test.go，6 sub-test）
- [ ] 补 Vue store 损坏缓存恢复、SecuritySettings 交互/i18n、关键上传和播放流程的组件测试
- [ ] 在启用 CGO 的 CI 任务运行 go test -race ./...
